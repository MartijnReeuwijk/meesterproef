<%- include('./partials/head'); -%>

<main class="displayFlex">
  <%- include('./partials/clock'); -%>


  <% for(var i = 0;  i < clips.length; i++) { %>
    <div class="mainpageImage previewImage transition">
      <!-- Test data but the path will be the same just change the number to the data Var -->
      <!-- The _0.png must stay so the the app will alwaya get the first one -->
  <!-- probs rebuild this page when we got the design since this is a kinda bad user interaction -->
    <img src="../images/thumbnails/thumbnails_large/<%=clips[i]%>/<%=clips[i]%>_0.png" alt="">

<!-- ${clips[1]}/${clips[1]} -->
    </div>
<% } %>

</main>

<%- include('./partials/footer'); -%>
